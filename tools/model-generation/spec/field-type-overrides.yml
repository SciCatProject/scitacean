# Specify types of fields.
# By default, types are taken from the schemas.
# Entries in this file override the specified types.
# Field names must be SciCat names (camelCase).

Dataset:
  history: None  # because history is dropped (see field-validations.yml)
  inputDatasets: List[PID]
  pid: PID
  sourceFolder: RemotePath
  type: DatasetType
Datablock:
  datasetId: PID
OrigDatablock:
  datasetId: PID
Relationship:
  pid: PID
